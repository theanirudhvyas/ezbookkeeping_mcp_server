[project]
name = "ezbookkeeping-mcp-server"
version = "0.1.0"
description = "MCP server for bookkeeping and financial management"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.21.2",
]
